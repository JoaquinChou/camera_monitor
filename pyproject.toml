[build-system]
requires = [
    "setuptools>=68",
    "wheel"
]
build-backend = "setuptools.build_meta"


[project]
name = "camera-monitor"
version = "0.0.1"
description = "Camera monitor"

requires-python = ">=3.9"


[tool.setuptools]
packages = [
    "models",
    "utils",
    "tasks"
]